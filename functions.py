def damnit(arch):
    return "Damn it, I'm a " + arch + "!" + arch[0:2] + "!"

def raynolds(user):
    if user == "admin":
        return "this is the password of the user" + user + "1234"
    else:
        return "this is the password of the user" + user + "2341"

